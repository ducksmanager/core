{
  "name": "ducksmanager",
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  },
  "workspaces": [
    "apps/*",
    "apps/duckguessr/api",
    "apps/whattheduck-ionic/packages/*",
    "packages/*"
  ],
  "dependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "build": "pnpm run -r build",
    "prepare": "husky install"
  },
  "devDependencies": {
    "bun-types": "^1.0.18"
  }
}