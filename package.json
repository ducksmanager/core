{
  "name": "ducksmanager",
  "dependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "dev": "pnpm run --filter web start",
    "publish": "pnpm --filter api run generate-route-types && pnpm --filter web publish",
    "prepare": "husky install"
  }
}
