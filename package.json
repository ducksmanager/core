{
  "name": "ducksmanager",
  "dependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "dev": "concurrently -n web,api \"pnpm run --filter web... dev\" \"pnpm run --filter api... dev\"",
    "build": "pnpm run -r build",
    "prepare": "husky install"
  }
}
