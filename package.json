{
  "name": "ducksmanager",
  "engines": {
    "node": ">=18.0.0 <19.0.0"
  },
  "dependencies": {
    "husky": "^9.0.10"
  },
  "scripts": {
    "build": "pnpm run -r build",
    "prepare": "husky install"
  }
}