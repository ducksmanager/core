{
  "name": "ducksmanager",
  "dependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "dev": "concurrently -n front,back \"pnpm --filter web run start\" \"pnpm --filter api run start\"",
    "publish": "pnpm --filter api run generate-route-types && pnpm --filter web publish",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint": "^8.47.0",
    "@typescript-eslint/eslint-plugin": "^5.62.0",
    "@typescript-eslint/parser": "^5.62.0",
    "typescript": "^5.2.2"
  }
}
