{
  "name": "ducksmanager",
  "dependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "dev": "pnpm run --filter web --filter api... dev",
    "build": "pnpm run -r build",
    "prepare": "husky install"
  }
}
