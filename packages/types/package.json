{
  "name": "~dm-types",
  "engines": {
    "node": ">=18.0.0 <19.0.0"
  },
  "module": "index.ts",
  "scripts": {
    "build": "rm -rf dist && ./node_modules/.pnpm/node_modules/.bin/tsc"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "~prisma-schemas": "workspace:*"
  }
}