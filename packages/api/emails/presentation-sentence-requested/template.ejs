<% for (const decision of ['approve', 'refuse'] ) { %>
    <a href="<%= process.env.WEBSITE_ROOT %>/admin/presentation-text/<%= decision %>?userId=<%= user.id %>&sentence=<%= encodeURIComponent(presentationText) %>"><%= decision %>%></a>
<% } %>
