{
  "name": "~prisma-clients",
  "module": "index.ts",
  "scripts": {
    "prisma:pull": "ls schemas/*.prisma | while read -r schema; do pnpx prisma db pull --schema=$schema; done",
    "dev": "pnpm run build",
    "build": "ls schemas/*.prisma | while read -r schema; do pnpx prisma generate --schema=$schema & done"
  },
  "dependencies": {
    "@prisma/client": "5.2.0",
    "prisma": "^5.2.0"
  }
}
