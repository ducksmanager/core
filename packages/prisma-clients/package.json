{
  "name": "prisma-clients",
  "module": "index.ts",
  "scripts": {
    "prisma:pull": "ls schemas/*.prisma | while read -r schema; do pnpx prisma db pull --schema=$schema; done",
    "build": "rm -rf $PWD/client_* && ls schemas/*.prisma | while read -r schema; do pnpx prisma generate --schema=$schema; done && mv dist/prisma/* ."
  },
  "dependencies": {
    "prisma": "^5.2.0"
  }
}
