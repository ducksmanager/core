version: '2.4'
services:
  front:
    container_name: ducksmanager-web-vue3
    image: ghcr.io/bperel/ducksmanager-web:latest
    depends_on:
      - php-flex
    restart: always
    networks:
      - workspace_global-front-network
      - dm-network

  php-flex:
    container_name: ducksmanager-php-vue3
    image: ghcr.io/bperel/ducksmanager-php:latest
    restart: always
    volumes:
      - dm_sessions:/tmp/sessions
    networks:
      - dm-network
      - dm-server_front-network

  sentry-relay:
    image: getsentry/relay
    restart: always
    container_name: sentry-vue3
    volumes:
      - ./config/sentry-relay:/work/.relay
    networks:
      - workspace_global-front-network


networks:
  dm-network:
  workspace_global-front-network:
    external: true
  dm-server_front-network:
    external: true

volumes:
  dm_sessions:
    external: true
