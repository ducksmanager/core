version: "3.2"

services:
  edges:
    image: "ghcr.io/bperel/dm-edges"
    container_name: edges
    restart: always
    env_file:
      - .env.prod
    networks:
      - public-network

networks:
  public-network:
    external: true
